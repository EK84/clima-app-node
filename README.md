## Aplicación del Clima -Curso Node

Esta aplicación permite saber la temperatura de un lugar a traves de la línea de comandos. Para ello debemos introducir el siguiente comando:

    - node app -d "Madrid"

Recordar antes hacer un npm install.